# SWE Coding Agent — dependencies
# Python 3.10+ required (floor set by agent-framework, redis, typer)
# Nothing is installed yet — see planning.md M0.

# --- Core orchestration ---
# Microsoft Agent Framework. The PyPI distribution is `agent-framework`, an
# umbrella package that pulls in core + all optional integrations (Azure AI,
# OpenAI, Copilot Studio, DevUI, etc.). If the install footprint gets painful,
# swap this for `agent-framework-core==1.13.0` plus only the extras we use.
agent-framework-core>=1.13.0

# --- Model provider client ---
# OpenAI SDK, used as the OpenAI-compatible client for Groq (base_url override).
openai>=1.0
# --- GitHub API ---
PyGithub==2.9.1

# --- Sandboxed execution ---
docker==7.2.0

# --- Trajectory memory ---
redis>=6.4.0,<7.2.1
# fakeredis backs the default (offline) test suite for agent/memory — no real
# Redis needed to run `pytest tests/ -m "not integration"`.
fakeredis>=2.30.0
# Local embedding model for trajectory similarity search (M7). Free, no API
# key, runs on CPU. Downloads its weights from Hugging Face Hub on first use
# (see agent/memory/embeddings.py for the offline/timeout handling of that).
sentence-transformers>=3.0.0

# --- Config / secrets ---
python-dotenv==1.2.2
pydantic>=2.0

# --- Safety / refusal layer (M6) ---
# configs/safety_policy.yaml is parsed by agent/safety/risk_classifier.py.
PyYAML>=6.0

# --- Evaluation & testing ---
pytest==9.1.1
pytest-cov==7.1.0
tiktoken==0.13.0

# --- CLI ---
typer==0.27.1
rich==15.0.0
